import { OnlyNumberOrDecimalDirective } from './only-number-or-decimal.directive';

describe('OnlyNumberOrDecimalDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlyNumberOrDecimalDirective();
    expect(directive).toBeTruthy();
  });
});
